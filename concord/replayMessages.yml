flows:
  replayMessages:
    - form: replaySource
      values:
        executedBy: "${currentUser.username}"
        organization: ${organization}
        processId: ${txId}
    - checkpoint: "Processing Request ..."

    - expr: ${replaySource.messageIds.stream().flatMap(str -> str.split(",").stream()).toList()}
      out: messageIdentifiers
    - expr: ${replaySource.facilities.stream().flatMap(str -> str.split(",").stream()).toList()}
      out: facilityNumbers
    - call: parse-url-definition

