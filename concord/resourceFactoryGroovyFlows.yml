flows:
  parse-url-definition:
    - script: groovy
      body: |
        def uri = "${replaySource.sourceUrl}"
        
        println "Apply Substitutions to uri: ${uri}\n"
        def uriToParse = applySubstitutions(replaySource.sourceUrl)
