flows:
 get-source-message:
   - log: "Preparing to perform a request to Source: ${} for reprocessing messages ..."
   - script: groovy
     body: |
        def splitDefinition = replaySource.sourceUrl.split('/')
        println('Size of splitDefinition ' + splitDefinition.size())